// Defined a value within a variable to be multiplied
const value = 3;

// Function takes in num parameter and multiplies the input by 2
function multiply(num) {
  const result = num * 2;
  return result;
}

// Logs result of multiply function with value number
console.log(multiply(value));